import { Roboto, Mulish } from "next/font/google";

export const mulish = Mulish({
  weight: ["300", "400", "700"],
  subsets: ["latin"],
});

export const roboto = Roboto({
  weight: ["500"],
  subsets: ["latin"],
});
